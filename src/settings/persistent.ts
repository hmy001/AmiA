const CACHE_TIME = 60 * 60 * 24 * 30; // 默认存储时间
const PREFIX_KEY = 'amia'; // 存储的键 前缀
const PATH = null; // 用于cookie的path
const DOMAIN = null; // 用于cookie的domain
const SECURE = false; // 用于cookie的secure true代表cookie只在https下传输
export default {
  CACHE_TIME,
  PREFIX_KEY,
  PATH,
  DOMAIN,
  SECURE,
};
